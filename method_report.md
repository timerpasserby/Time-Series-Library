# 方法与模型报告

## 数据集
- **数据集**：露天煤矿边坡多源异构模拟数据 (`gemi_sim.py` 版)
- **特征组织**：按源解耦为三份文件（雷达 / 天气 / 爆破），不再强制拼接为单表
- **目标列**：当前训练配置支持全节点联合预测
- **时间频率**：小时级 (H)
- **任务类型**：长期多变量预测 (Long-term forecasting)

## 数据处理方法更新
- `process_radar_data.py` 已删除 `pd.merge` 路径。
- 雷达数据输出为纯节点序列：`date + node_0...node_999`。
- 天气与爆破分别独立保存为 `weather.csv` 与 `blast.csv`，用于后续按需拼接。

## 仿真方法更新
- `gemi_sim.py` 已增大单次位移量级，基础位移、降雨响应、爆破响应和余震滞后项均已放大。
- 降雨响应现在同时考虑累积降雨、当前降雨和高湿度软化，并加入湿润阈值放大。
- 爆破响应现在采用更缓的距离衰减，并增加基础传播项，确保更大范围节点可见爆破扰动。
- 雷达位移文件已显式写出 `report_time` 列，便于与天气、爆破日志做时间对齐。

## 评估模型
计划评估以下模型：
1. **TimesNet**：通过 2D 变分建模捕捉周期性任务。
2. **PatchTST**：基于 Patch 的通道独立 Transformer 模型。
3. **TimeFilter**：具有高效时空相关性捕捉能力的 Patch-Specific 模型。

## 评估指标
- **MSE** (Mean Squared Error)
- **MAE** (Mean Absolute Error)

## 当前状态
模型启动遇到 `patoolib` 依赖缺失问题，正在等待依赖项解决。
